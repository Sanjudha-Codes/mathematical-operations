class Solution:
    def gcd(self, a, b):
        if a<b:
            k=a
        else:
            k=b
        sum=1        
        for i in range(1,k+1):
            if(a%i==0 and b%i==0):
                sum=i
        return sum        
